# Create a tuple called `my_fav_numbers` with your favorites numbers.
# Add two new numbers to it.
# Create a tuple called `friend_fav_numbers` with your friend's favorites numbers.
# Concatenate `my_fav_numbers` and `friend_fav_numbers` to `our_fav_numbers`.

my_fav_numbers = (8, 12, 15)
friend_fav_numbers = (5, 12, 34, 1)

our_fav_numbers = my_fav_numbers + friend_fav_numbers

print(our_fav_numbers)
